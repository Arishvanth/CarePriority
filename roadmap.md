# Roadmap

- [ ] Make referred consultation referral writes deterministic and idempotent.
- [ ] Enforce the referred status invariant in the database.
- [ ] Correct only pending referrals linked to referred consultations.
- [ ] Verify live database status and dashboard counts after refresh-equivalent reads.
- [ ] Step 7: Doctor completion must resolve the patient's open consultation instead of page state.
- [ ] Step 7: Observation exit must use the admission event's consultation link.
